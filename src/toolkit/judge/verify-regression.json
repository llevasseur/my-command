{
  "set": "verify-regression",
  "version": "1.0.0",
  "kind": "noul",
  "title": "Pre-existing failure, or a regression from this branch",
  "description": "One noul over two verify log tails. It judges provenance only, once both states are already in hand.",
  "acts": false,
  "actsNote": "Nothing acts on an answer from this set. Nothing-acting is the safety mechanism, not shadow mode. See docs/adrs/0008-no-question-set-acts-in-this-campaign.md.",
  "wiredInto": [],
  "batch": false,
  "sources": ["docs/specs/command-toolkit.md"],
  "adrs": [
    "docs/adrs/0008-no-question-set-acts-in-this-campaign.md",
    "docs/adrs/0010-eval-harness-before-the-layer.md",
    "docs/adrs/0012-two-eval-subjects-not-five.md",
    "docs/adrs/0017-a-verify-failure-carries-its-own-provenance.md"
  ],
  "scope": {
    "statement": "THE EXPENSIVE HALF OF THIS PROBLEM IS NOT THE JUDGEMENT. It is acquiring the baseline state — checking out the default branch, booting it, and running the gates again — and that wants a caching verb rather than a classifier. This set judges only once BOTH states are already in hand. A run that has only the branch's own failure has nothing for this set to answer and must acquire the baseline first.",
    "note": "A classifier asked to guess the baseline it was never given would be answering a different, easier question and scoring well on it."
  },
  "state": {
    "carries": [
      "the verify log tail from this branch",
      "the verify log tail from the same gate on the default branch",
      "the changed-file list for the branch"
    ],
    "note": "Both tails are required. Conversation-derived state leaving the device is gated twice, off by default, and printable with --dry-run.",
    "adr": "docs/adrs/0009-conversation-derived-state-leaves-the-device.md"
  },
  "eval": {
    "labels": "none",
    "statement": "THIS SET CARRIES NO EVAL NUMBERS. Do not look for one elsewhere; none exists. What changed is why: the label is no longer absent by construction, only unaccumulated. verdict.json used to record red or green for a RUN and nothing per failure, so no surviving run could contain this set's label whatever else happened. It now records each failure on its own — the gate, what it said, and an id derived from those two — with its provenance unset, and `my-command-tools shots resolve --failure <id> --provenance regression|pre-existing` writes the answer onto that id once a fix or a look at the default branch settles it. A resolved failure is exactly this set's label, sitting beside the run it came from.",
    "corpusNote": "NO CORPUS EXISTS YET, and recording the structure does not create one. Labels accumulate only as people actually resolve failures, none has been resolved, and the screenshot keep still ages a run out at 7 days, which bounds how long a resolved failure survives to be counted. Read this field again before quoting a number from it.",
    "recordedBy": "docs/adrs/0017-a-verify-failure-carries-its-own-provenance.md",
    "adr": "docs/adrs/0012-two-eval-subjects-not-five.md"
  },
  "questions": [
    {
      "id": "IS_REGRESSION",
      "noul": "This failure is a regression introduced by the branch under test, rather than a failure that is already present on the default branch.",
      "answerMeans": "1.0 means the branch introduced it; 0.0 means it was already failing on the default branch before this branch existed.",
      "requires": ["branchLogTail", "baselineLogTail"],
      "why": "The same failure text can be produced by a branch's own change or by a gate that was already red, and telling them apart is a comparison between two log tails rather than a property of either one."
    }
  ]
}
